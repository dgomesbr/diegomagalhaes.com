#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../groc/bin/groc" "$@"
  ret=$?
else 
  node  "$basedir/../groc/bin/groc" "$@"
  ret=$?
fi
exit $ret
